<!DOCTYPE html>
<html>
    <head>
        <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css'>
        <link rel='stylesheet' href='css/styles.css'>
        <title>Catalog</title>
    </head>
    <body>
        <div class='container'>
            <h1>Library catalog</h1>

            {% if books is not empty %}
        <p>Complete Catalog</p>
        <ul>
            {% for book in books %}
                <li><a href = '/books/{{book.getBookId}}'>{{ book.getTitle }}</a></li>
            {% endfor %}
        </ul>
    {% endif %}

    <h4>Add Book to collection:</h4>

    <form action='/add_books' method='post'>
        <label for='book_title'>Title</label>
        <input type='text' name='book_title' id='book_title' placeholder='Add New Book'>
        <button type='submit'>Add Book</button>
    </form>

    <form action='/search' method='post'>
        <label for='search'>Search by book and or author!</label>
        <input type='text' name='search' id='search' placeholder='Search Book'>
        <input type='text' name='author_search' id='author_search' placeholder='Search Author'>
        <button type='submit'>Search</button>
    </form>


        </div>

    </body>
</html>
